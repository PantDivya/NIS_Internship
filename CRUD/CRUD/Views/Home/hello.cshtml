@model IEnumerable<CRUD.Models.tblEmployee>
@{
    ViewBag.Title = "hello";
}

<h2>hello</h2>

@foreach(var item in Model)
            {
                <p>@item.Id</p>
                <p>@item.Name</p>
                <p>@item.Address</p>
            }

