@model CRUD.Models.Product
@{
    ViewBag.Title = "hello";
}

<h2>Product</h2>

<h3>LINQ query</h3>

<h4>4.Write a LINQ query to select only the names of all products.</h4>

@*@if (false)
{
    <p style="color:red">There is no data with category "Toys"</p>
}*@


<div class="container border" id="query_1">
    <div class="mt-3">
        <h2>_ProductList</h2>
    </div>
    <table class="table table-striped table-bordered border-secondary">
        <thead class="table-dark">
            <tr>
                <th scope="col">Product Id</th>
                <th scope="col">Product Name</th>
                <th scope="col">Category</th>
                <th scope="col">Price</th>
                <th scope="col">Stock</th>
            </tr>
        </thead>
        <tbody>

           @* @foreach (var item in Model)
            {*@
                <tr>
                    <td>@Html.DisplayFor(m => m.ProductId)</td>
                    <td>@Html.DisplayFor(m => m.Name)</td>
                    <td>@Html.DisplayFor(m => m.Category)</td>
                    <td>@Html.DisplayFor(m => m.Price)</td>
                    <td>@Html.DisplayFor(m => m.Stock)</td>
                </tr>
            @*}*@
        </tbody>
    </table>
</div>




