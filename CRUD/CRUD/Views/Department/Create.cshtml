@model CRUD.Models.tblDepartment
@{
    ViewBag.Title = "Create";
}

@if (ViewData.ModelState.IsValid)
{
    if (@ViewBag.Name != null)
    {
        <b>
            Id : @ViewBag.Id<br />
            DepartmentName : @ViewBag.DepartmentName<br />
        </b>
    }
}
<!--Create Department-->

<div class="container mt-3 border">
    <div class=" mt-3 mb-3 m-5">
        <h3>Add New Department</h3>
    </div>
    @using (@Html.BeginForm("AddDepartment", "Department", FormMethod.Post))
    {
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.AntiForgeryToken()

        @*@Html.HiddenFor(m=>m.Id)*@

        @*<div class="form-group m-5 mb-3 mt-3 col-md-10">
            @Html.LabelFor(m => m.Id, new { @class = "form-label" })
            @Html.TextBoxFor(m => m.Id, new { @class = "form-control", @placeholder = "Enter Name" })
            @Html.ValidationMessageFor(model => model.Id, "", new { @class = "text-danger" })

        </div>*@
        <div class="form-group m-5 mb-3 mt-3 col-md-10">
            @Html.LabelFor(m => m.DepartmentName, new { @class = "form-label" })
            @Html.TextBoxFor(m => m.DepartmentName, new { @class = "form-control", @placeholder = "Enter Address" })
            @Html.ValidationMessageFor(m => m.DepartmentName, "", new { @class = "text-danger" })
        </div>
        <div class="form-group m-5 mb-3 mt-3">
            <input type="submit" value="Create" class="btn btn-success" />
        </div>

    }

</div>

